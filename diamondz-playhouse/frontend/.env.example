# Example environment variables for Diamondz Playhouse frontend
# Copy to `.env.local` (or your framework's preferred local env file) and fill values.

# Frontend API + Stripe publishable key
REACT_APP_API_BASE_URL=http://localhost:5000
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Optional: feature toggles or front-facing configs
REACT_APP_FEATURE_X=true

# Notes:
# - Do not commit secrets or `.env.local` to source control.
# - If your frontend uses Vite or CRA, check docs for the proper `.env` filename and prefix requirements.VITE_API_URL=http://localhost:5000/api
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
